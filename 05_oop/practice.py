"""练习：用 @dataclass 定义 User(id: int, name: str, active: bool=True)"""

from dataclasses import dataclass

# TODO: 定义 User


if __name__ == "__main__":
    # 取消注释并跑通
    # user = User(1, "lei")
    # assert user.active is True
    print("TODO: 完成 User dataclass 后取消注释")
